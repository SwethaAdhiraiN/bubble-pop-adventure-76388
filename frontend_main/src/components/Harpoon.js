import React from 'react';

// PUBLIC_INTERFACE
function Harpoon() {
  /**
   * Represents the harpoon shot by the player character.
   * To be placed dynamically within the GameCanvas.
   */
  return (
    <div className="harpoon-placeholder">
      |
    </div>
  );
}

export default Harpoon;
