import React from 'react';

// PUBLIC_INTERFACE
function Character() {
  /**
   * Represents the player's character.
   * To be rendered and animated within the GameCanvas.
   */
  return (
    <div className="character-placeholder">
      👾
    </div>
  );
}

export default Character;
